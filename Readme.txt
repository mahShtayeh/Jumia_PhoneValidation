The project can be imported into STS IDE and booted locally using it's dashboard 
The sample data base have been placed in the active directory of the project 
The project can be built and deployed using the commands 
    ./mvnw clean package docker:build 
